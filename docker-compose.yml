spark-master:
    image: robingu/spark
    ports: 
        - "8080:8080"
        - "7077:7077"
    command: start-master.sh && tail -f /opt/spark/logs/*.out
    hostname: spark-master

spark-slave:
    image: robingu/spark
    command: start-slave.sh spark://127.0.0.1:7077 && tail -f /opt/spark/logs/*.out 
    #ports:
    links:
        - spark-master:spark-master

    #secondarynamenode:
    #    image: robingu/spark-docker
    #    command: hdfs secondarynamenode
    #    ports:
    #        - "50090:50090"
    #    links:
    #        - namenode:hdfs-namenode
